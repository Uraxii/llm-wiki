# Known mutmut survivors for llmwiki_service/auth.py, tokens.py, and
# deployment.py.
#
# This is the mutation testing gate's ratchet: `check-mutation-gate.py`
# fails only when a survivor appears that is NOT on this list. Every
# entry here has been checked by hand and is an equivalent mutant: a
# change with no test-observable effect on behavior, so no test can
# kill it. The trailing `# reason` comment on each line is that check,
# not a note for later; an entry with no reason is not finished.
#
# Regenerate with this command. It rewrites the survivor list below
# while preserving this header and every entry's `# reason` comment:
#   .venv/bin/python scripts/check-mutation-gate.py --update-baseline
#
# A mutant that regenerates with no prior entry is written bare and
# printed to stderr as needing a reason. Read its diff with
# `mutmut show <id>` before adding one: if it turns out to be killable,
# write the test instead of a reason.
#
# Stability: each id is `<module>.<qualified_name>__mutmut_<N>`, where N
# is assigned in the order mutmut's mutation generator visits that one
# function. An edit inside a listed function (adding a branch, an
# operator, a call) can renumber every mutant after it in that same
# function, even though nothing about the surviving mutant's behavior
# changed. An edit to an unrelated function, or anywhere outside
# auth.py/tokens.py/deployment.py, does not renumber these ids. A red
# run after an edit to a function named below is expected: regenerate
# the baseline, read the diff, confirm every entry that changed id
# still represents the same surviving mutation, and only then commit it.
llmwiki_service.app.x_build_app__mutmut_11  # dropping the methods= keyword entirely equals methods=None: Route.__init__'s methods parameter already defaults to None, so this is the same code path as mutmut_8, not a second gap
llmwiki_service.app.x_build_app__mutmut_15  # methods=["get"] equals methods=["GET"]: Route.__init__ does self.methods = {method.upper() for method in methods}, so the lowercase entry builds the identical {"GET", "HEAD"} set, verified empirically
llmwiki_service.app.x_build_app__mutmut_8  # methods=None equals methods=["GET"] for a function endpoint: Route.__init__ has `if methods is None: methods = ["GET"]` before it builds self.methods, so both reach the identical {"GET", "HEAD"} set, verified empirically against the installed starlette
llmwiki_service.app.x_forwarded_elements__mutmut_5  # decode("LATIN-1") equals decode("latin-1"): Python's codec lookup lowercases and normalizes hyphens before matching an alias, so both resolve to the same iso8859-1 codec, verified empirically (b"\xff".decode("LATIN-1") == b"\xff".decode("latin-1"))
llmwiki_service.auth.xǁFailureLimiterǁ_forget_expired__mutmut_6  # break->return: nothing follows the while loop in _forget_expired, so returning early on the first still-open window behaves identically to breaking out of the loop
llmwiki_service.auth.xǁFailureLimiterǁ_forget_expired__mutmut_7  # popitem(last=None) equals popitem(last=False): OrderedDict.popitem treats `last` as a plain truthiness check in CPython, and None is falsy the same as False
llmwiki_service.auth.xǁSearchLimiterǁ_forget_expired__mutmut_10  # popitem(last=None) equals popitem(last=False): OrderedDict's last kwarg only tests truthiness, and bool(None) is False
llmwiki_service.auth.xǁSearchLimiterǁ_forget_expired__mutmut_12  # continue->break equals continue here: the retained entries are an expired prefix, and the cap can only refuse when that prefix is empty and the two tables are identical
llmwiki_service.auth.xǁSearchLimiterǁ_forget_expired__mutmut_13  # break equals return here: nothing follows the while loop, so falling through and returning None are identical
llmwiki_service.deployment.x__admin_row_exists__mutmut_11  # sqlite3.connect(..., uri=True) dropped or changed: this build's SQLite (3.53.4, compiled with SQLITE_USE_URI) parses the "file:" prefix and its mode=ro query param on every connect regardless of the uri= flag, verified empirically; the mutated call opens the same read-only view of the same file
llmwiki_service.deployment.x__admin_row_exists__mutmut_12  # same SQLITE_USE_URI equivalence as mutmut_11, with uri=False instead of dropped
llmwiki_service.deployment.x__admin_row_exists__mutmut_16  # SQL keyword casing: sqlite parses keywords and unquoted identifiers case-insensitively, so the lowercased query is behaviorally identical
llmwiki_service.deployment.x__admin_row_exists__mutmut_19  # same SQL casing equivalence as mutmut_16
llmwiki_service.deployment.x__admin_row_exists__mutmut_20  # same SQL casing equivalence as mutmut_16
llmwiki_service.deployment.x__admin_row_exists__mutmut_9  # same SQLITE_USE_URI equivalence as mutmut_11, with uri=None instead of dropped
llmwiki_service.routes.x_make_routes__mutmut_120  # response.headers["retry-after"] equals "Retry-After": starlette's MutableHeaders.__setitem__ does key.lower().encode("latin-1"), so all three spellings write the identical b"retry-after" wire header, verified empirically
llmwiki_service.routes.x_make_routes__mutmut_121  # response.headers["RETRY-AFTER"] equals "Retry-After": starlette's MutableHeaders.__setitem__ does key.lower().encode("latin-1"), so all three spellings write the identical b"retry-after" wire header, verified empirically
llmwiki_service.routes.x_make_routes__mutmut_21  # request.headers.get("AUTHORIZATION") equals the lowercase key: starlette's Headers.__getitem__ does key.lower().encode("latin-1") before scanning the raw header list, so both select the same header, verified empirically against the installed starlette 1.6.0
llmwiki_service.routes.x_make_routes__mutmut_217  # headers={"CONTENT-TYPE": ...} equals "content-type": starlette's Response.init_headers lowercases every key while building raw_headers (and checks its own populate_content_type against the lowercased set), so the wire header is identical, verified empirically in page_route
llmwiki_service.routes.x_make_routes__mutmut_365  # headers={"CONTENT-TYPE": ...} equals "content-type": starlette's Response.init_headers lowercases every key while building raw_headers (and checks its own populate_content_type against the lowercased set), so the wire header is identical, verified empirically in schema_route
llmwiki_service.routes.x_make_routes__mutmut_370  # methods=None equals methods=["GET"] for a function endpoint: Route.__init__ has `if methods is None: methods = ["GET"]` before it builds self.methods, so both reach the identical {"GET", "HEAD"} set; same equivalence already recorded for app.x_build_app__mutmut_8, re-verified empirically here (search route)
llmwiki_service.routes.x_make_routes__mutmut_373  # dropping the methods= keyword equals methods=None, which Route.__init__ then turns into ["GET"] for a function endpoint; same code path as the methods=None entry above (search route)
llmwiki_service.routes.x_make_routes__mutmut_377  # methods=["get"] equals methods=["GET"]: Route.__init__ does self.methods = {method.upper() for method in methods}, so the lowercase entry builds the identical {"GET", "HEAD"} set, verified empirically (search route)
llmwiki_service.routes.x_make_routes__mutmut_380  # methods=None equals methods=["GET"] for a function endpoint: Route.__init__ has `if methods is None: methods = ["GET"]` before it builds self.methods, so both reach the identical {"GET", "HEAD"} set; same equivalence already recorded for app.x_build_app__mutmut_8, re-verified empirically here (page route)
llmwiki_service.routes.x_make_routes__mutmut_383  # dropping the methods= keyword equals methods=None, which Route.__init__ then turns into ["GET"] for a function endpoint; same code path as the methods=None entry above (page route)
llmwiki_service.routes.x_make_routes__mutmut_387  # methods=["get"] equals methods=["GET"]: Route.__init__ does self.methods = {method.upper() for method in methods}, so the lowercase entry builds the identical {"GET", "HEAD"} set, verified empirically (page route)
llmwiki_service.routes.x_make_routes__mutmut_390  # methods=None equals methods=["GET"] for a function endpoint: Route.__init__ has `if methods is None: methods = ["GET"]` before it builds self.methods, so both reach the identical {"GET", "HEAD"} set; same equivalence already recorded for app.x_build_app__mutmut_8, re-verified empirically here (list route)
llmwiki_service.routes.x_make_routes__mutmut_393  # dropping the methods= keyword equals methods=None, which Route.__init__ then turns into ["GET"] for a function endpoint; same code path as the methods=None entry above (list route)
llmwiki_service.routes.x_make_routes__mutmut_397  # methods=["get"] equals methods=["GET"]: Route.__init__ does self.methods = {method.upper() for method in methods}, so the lowercase entry builds the identical {"GET", "HEAD"} set, verified empirically (list route)
llmwiki_service.routes.x_make_routes__mutmut_400  # methods=None equals methods=["GET"] for a function endpoint: Route.__init__ has `if methods is None: methods = ["GET"]` before it builds self.methods, so both reach the identical {"GET", "HEAD"} set; same equivalence already recorded for app.x_build_app__mutmut_8, re-verified empirically here (schema route)
llmwiki_service.routes.x_make_routes__mutmut_403  # dropping the methods= keyword equals methods=None, which Route.__init__ then turns into ["GET"] for a function endpoint; same code path as the methods=None entry above (schema route)
llmwiki_service.routes.x_make_routes__mutmut_407  # methods=["get"] equals methods=["GET"]: Route.__init__ does self.methods = {method.upper() for method in methods}, so the lowercase entry builds the identical {"GET", "HEAD"} set, verified empirically (schema route)
llmwiki_service.tokens.x_new_token__mutmut_4  # secrets.token_urlsafe(None) equals secrets.token_urlsafe(SECRET_BYTES): secrets.DEFAULT_ENTROPY is 32, the same value as SECRET_BYTES today, so the default-entropy call yields the same output length and randomness; would stop being equivalent only if SECRET_BYTES were changed to diverge from the stdlib default
llmwiki_service.tokens.x_token_id__mutmut_1  # len(token) <= CHECKSUM_CHARS changed to <: a token of exactly CHECKSUM_CHARS length always has an empty body (token[:-CHECKSUM_CHARS] == ""), and "" can never split into the three "_"-joined parts token_id requires, so the mutant still returns None for every such input
llmwiki_service.tokens.xǁTokenStoreǁ_connect__mutmut_5  # PRAGMA casing: sqlite PRAGMA names and values are case-insensitive, so the lowercased pragma is behaviorally identical
llmwiki_service.tokens.xǁTokenStoreǁ_connect__mutmut_6  # same PRAGMA casing equivalence as mutmut_5
llmwiki_service.tokens.xǁTokenStoreǁ_stamp_use__mutmut_15  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁ_stamp_use__mutmut_16  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁ_stamp_use__mutmut_18  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁ_stamp_use__mutmut_19  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁ_stamp_use__mutmut_8  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁ_stamp_use__mutmut_9  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁlist_tokens__mutmut_5  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁlist_tokens__mutmut_6  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁlist_tokens__mutmut_8  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁlist_tokens__mutmut_9  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁmint__mutmut_20  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁmint__mutmut_21  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁmint__mutmut_23  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁmint__mutmut_24  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁrevoke__mutmut_11  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁrevoke__mutmut_12  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁrevoke__mutmut_8  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁrevoke__mutmut_9  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁverify__mutmut_13  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁverify__mutmut_14  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁverify__mutmut_16  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁverify__mutmut_17  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁverify__mutmut_19  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁverify__mutmut_20  # SQL casing equivalence, same reasoning as x__admin_row_exists__mutmut_16
llmwiki_service.tokens.xǁTokenStoreǁverify__mutmut_38  # _stamp_use's version arg forced to None: version is only read in `if version == current`; forcing the else branch recomputes token_hash under peppers.keys[current] and rewrites pepper_version to current, which are exactly the values already stored when version was truly current, so the row ends up identical either way
