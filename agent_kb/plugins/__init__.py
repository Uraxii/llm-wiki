"""Plugin modules, discovered by `agent_kb.plugin.discover`.

One module per query against one connector. A module is a plugin if it
has a module-level `NAME` and a module-level `collect`; modules whose
name starts with `_` are skipped.
"""
