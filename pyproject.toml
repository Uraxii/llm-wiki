[project]
name = "llm-wiki"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["readability-lxml", "lxml"]

[project.optional-dependencies]
test = ["pytest"]

[project.scripts]
llm-wiki = "llm_wiki:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
