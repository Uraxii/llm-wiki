__pycache__/
*.pyc
.venv/
.env
*.db
*.sqlite3
.DS_Store

# Beads / Dolt files (added by bd init)
.dolt/
.beads-credential-key
.beads/proxieddb/

# session handoffs, never committed
.handoffs/
scratch/

# decision log, kept local per the user
docs/plans/01-llm-wiki-poc/decisions.tsv
.nikki-agents/

# mutmut's working copy and per-mutant test cache (scripts/check-mutation-gate.py)
mutants/

# secrets and keys, so a stray file cannot be committed into a public repo
.env.*
*.sqlite
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519
.netrc
.direnv/
